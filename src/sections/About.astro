---
import { Image } from 'astro:assets';
import aboutImg from "../assets/about.png";

---
<section class="about" id="about">
    <div class="about__img">
        <Image src={aboutImg} class={"about-img"} alt="about-image"/>
    </div>
    <div class="about__texts">
        <h2 class="about__title">Ready To Make A <br> Change?</h2>
        <p class="about__paragraph">Taking the first step towards a healthier, stronger you can be the most challenging part of the journey, but it's also the most rewarding. Whether you're a beginner or a seasoned athlete, our personalized training programs are designed to help you reach your goals faster and more efficiently.</p>

        <p class="about__paragraph">With our motivating trainers, energizing classes, and state-of-the-art equipment, you'll have everything you need to stay committed and see real results.</p>
        <a class="about__btn" href="#">Get Started</a>
    </div>
</section>

<style>
    .about{
        max-width: var(--max-width);
        margin: 0 auto;
        display: flex;
        justify-content: space-between;
        gap: 3rem;
        padding: 5rem 2rem;
        align-items: center;
    }
    .about__texts{
        display: flex;
        flex-direction: column;
        gap: 1rem;
        width: 90%;
    }
    .about__title{
        color: var(--text-dark);
        font-weight:800;
        font-size: 2rem;
        
    }
    .about__paragraph{
        color: var(--text-light);
        line-height: 1.5;
        font-size: 1.1rem;
    }
    .about__btn{
        padding: 0.8rem 1.5rem;
        border-radius: 5px;
        background-color: var(--text-dark);
        color: var(--white);
        cursor: pointer;
        font-size: 1rem;
        transition: 0.3s;
        width: 30%;
        text-align: center;
        
    }
    .about__btn:hover{
        background-color: var(--primary-color);
    }
    .about__img{
        width: 100%;
        background: var(--gradient-2);
        padding: 2rem 1rem;
        border-radius: 8rem;
        display: flex;
        margin: 0 auto;
        padding-left: 2rem;
    }
    .about-img{
        width: 100%;
        height: 100%;
    }
    @media (max-width:768px ){
        .about{
        flex-direction: column;
    }
    .about__img{
        padding:1rem;   
        
    }
    .about-img{
        width: 100%;
    }
    .about__texts{
        width: 100%;
    }
    .about__btn{
        width: 20%;
    }
    @media (max-width:540px ){
        .about__btn{
            width: 40%;
        }
    }
    }
</style>

<script>
    import ScrollReveal from 'scrollreveal';
    const scrollRevealOption = {
  origin: "bottom",
  distance: "50px",
  duration: 1000,
};

ScrollReveal().reveal(".about__img img", {
  ...scrollRevealOption,
  origin: "left",
});

ScrollReveal().reveal(".about__title", {
  ...scrollRevealOption,
  origin: "bottom",
});

ScrollReveal().reveal(".about__paragraph", {
  ...scrollRevealOption,
  origin: "bottom",
  delay:500
});




</script>